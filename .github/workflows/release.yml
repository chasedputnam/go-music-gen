name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    name: Build and Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24.x'
          cache-dependency-path: go.sum

      - name: Build cross-platform binaries
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          echo "Building version: $VERSION"
          LDFLAGS="-s -w -X main.version=${VERSION}"

          mkdir -p build

          # Linux amd64
          GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -trimpath -ldflags "${LDFLAGS}" -o build/go-music-gen-linux-amd64 ./cmd/server

          # Linux arm64
          GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -ldflags "${LDFLAGS}" -o build/go-music-gen-linux-arm64 ./cmd/server

          # macOS amd64
          GOOS=darwin GOARCH=amd64 CGO_ENABLED=0 go build -trimpath -ldflags "${LDFLAGS}" -o build/go-music-gen-darwin-amd64 ./cmd/server

          # macOS arm64 (Apple Silicon)
          GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -trimpath -ldflags "${LDFLAGS}" -o build/go-music-gen-darwin-arm64 ./cmd/server

          # Windows amd64
          GOOS=windows GOARCH=amd64 CGO_ENABLED=0 go build -trimpath -ldflags "${LDFLAGS}" -o build/go-music-gen-windows-amd64.exe ./cmd/server

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            build/go-music-gen-linux-amd64
            build/go-music-gen-linux-arm64
            build/go-music-gen-darwin-amd64
            build/go-music-gen-darwin-arm64
            build/go-music-gen-windows-amd64.exe
            run.sh
